include details-findings-modal

mixin findingsTable(findings)
    if findings.length
        table(id="findingsTable" class="display").table.table-bordered
            thead
                tr
                    th Reference
                    th Project
                    th Title
                    th Type
                    th Severity
                    th Status
                    th Assignee
                    th Origin
                    th Reported By
                    th CVE
                    th CVSS
                    th Actions
            tbody
                each finding in findings
                    tr
                        td= finding.reference
                        td= finding.project
                        td= finding.title
                        td= finding.type
                        td= finding.severity
                        td= finding.status
                        td= finding.assignee
                        td= finding.origin
                        td= finding.reportedBy
                        td= finding.cve
                        td= finding.cvss
                        td
                            //- View button
                            button.btn.btn-info.btn-sm(data-bs-toggle="modal" data-bs-target=`#detailsModal-${finding._id}`)
                                i.fas.fa-search
                            //- Edit button
                            button.btn.btn-warning.btn-sm(data-bs-toggle="modal" data-bs-target=`#editFindingModal-${finding._id}`)
                                i.fas.fa-pencil-alt
                            button.btn.btn-danger.btn-sm(data-bs-toggle="modal" data-bs-target="#deleteFindingConfirmationModal" data-finding-id=`${finding._id}`)
                                i.fas.fa-trash-alt

                    +detailsModal(finding)
    else
        p No findings available.